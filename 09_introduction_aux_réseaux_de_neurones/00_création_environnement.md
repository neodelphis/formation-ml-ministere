Lancer la création avec la commande:

```bash
conda env create -f environment-tf.yml
```

 L'installation de TensorFlow étant généralement assez lourde, cette version minimaliste leur fera gagner un temps précieux lors de la configuration.

Se placer dans l'environnement nouvellement créé avec la commande:

```bash
conda activate tf
```